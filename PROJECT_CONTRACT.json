{
  "schema_version": 1,
  "metrics": {
    "production_loc": 2323,
    "production_files": 45,
    "production_packages": 12,
    "test_files": 9,
    "production_functions": 195,
    "decision_points": 334
  },
  "persistence": {
    "module": "go.etcd.io/bbolt",
    "entities": ["Record", "AuditEvent", "Workflow", "Attachment"],
    "reopen_test": "TestPersistenceSurvivesReopen"
  },
  "workflows": [
    {
      "steps": ["create", "review", "confirm", "archive"],
      "test": "TestWorkflowCreateReviewArchive"
    },
    {
      "steps": ["search", "select", "update", "publish"],
      "test": "TestWorkflowSearchUpdatePublish"
    },
    {
      "steps": ["import", "validate", "persist", "report"],
      "test": "TestWorkflowImportReport"
    }
  ]
}
